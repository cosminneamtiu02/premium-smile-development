# MIGRATION_INVENTORY — old repo → new atoms

> One row per old-repo piece, appended by each `/new-atom` run (skill S2).
> Verdicts: **rewrite** = rebuilt fresh to the new contract · **merge** = job
> absorbed into another component · **drop** = not migrated, covered elsewhere.
> Merge/drop verdicts require explicit owner approval (recorded per row).

| Old source (premium-smile-webpage) | Verdict | Lands as | Owner approval |
|---|---|---|---|
| `ui/button` (shadcn-style variant map; scale/translate hovers) | rewrite | `ui/Button` — slot children, `variant solid/outline/ghost`, `size md/lg/xl`, `asChild`, one calm color crossfade on a shared 400ms clock (nothing moves; outline fades ground + label together, accepted transient per the invariant in `Button.tsx`) | plan canvas 2026-08-02 (`.claude/plans/button-migration.plan.md`); v1 sweep discarded and replaced by the crossfade, canvas fb-37/fb-38 2026-08-04 (`.claude/plans/button-hover-fade.plan.md`) |
| Footer's 2 hand-rolled ANPC `<a>` buttons | merge → **superseded: drop 1, plain-link 1** (delivered shape) | ONE badge, not two: the SOL/ODR platform died 2025-07-20 and must never ship again; SAL points at the current portal (reclamatiisal.anpc.ro, Ordin 270/2026). Not `Button variant="outline"` as planned 2026-08-02 either — the official pictogram artwork IS the control: a plain `<a aria-label><img alt="">`, decorative image + named link (announced once), width/height attributes reserving the box (§11), lazy, rem-sized with `max-w-full`. Plain `<img>` over `ui/Image` for three independent reasons recorded in `Footer.tsx` (client-component hydration vs the band's zero-island contract; optimizer only covers `public/images`; its 404 recovery needs the JS point 1 refuses) — the fb-83 official-artwork fallback taken deliberately | plan canvas 2026-08-02; superseded by N2 composition contract fb-179, 2026-08-17 (SAL/SOL status research in-lane); sealed on owner "create pr" 2026-08-20 |
| Hero/TopBar "parent resizes button internals" pattern (`lg:[&>*]:h-16` …) | drop (pattern, not component) | `size` prop (§6.8) | canvas annotation fb-23, 2026-08-02 |
| `ui/rich-text` (bold-marker renderer) | drop | next-intl `t.rich()` at section level | canvas annotation fb-22, 2026-08-02 |
| `ui/icon-button` + `floating-book-cta`'s round face (its `fixed`/portal placement stays future locale-shell usage, fb-47) | merge | `ui/GlyphButton` (**renamed from `ui/RoundButton`**, top-bar rework #22) — icon slot (children, always inline SVG — normally `<Icon name>`; stories consume real glyphs, D8 round 5), `variant solid/outline/ghost` (ghost = transparent rest + `bg-line-subtle` dim tray, Button-ghost parity — the dossier's `bg-raised` superseded as white-on-white, ratified with the pack), **`shape round/square`** (geometry only, default round — square is the 6px burger cell), `size md 44px/lg 56px` fixed (§9 target; no `sm:` self-scaling, §6.5), morph-ready root (`group` marker + `aria-expanded` pass-through — transforms live with the Header's own svg child), `asChild` via **shared `ui/slot.ts`** (fb-64 — Button delegates too; standing rule + one G2 hardening recorded in that file), §6.3 typed REQUIRED `aria-label`, Button's calm 400ms fade — old `scale-105` + shadow pop dropped (fb-49/50) | plan canvas fb-41…fb-64 2026-08-04/05, approved fb-85; S7 pack approved in chat ("superb"), 2026-08-05; **rework:** Header board fb-106/118/124 (2026-08-05), G2 ×3 zero critical/high, 6 baselines re-keyed byte-identical + Ghost/SquareGhost added (IconSizePrecedence declared text diff), pack approved in chat 2026-08-06 (#22) |
| `app/body-overlay-scrollbar.tsx` + `overlayscrollbars`/`overlayscrollbars-react` deps + `.os-theme-smile-overlay` theme (page overlay scrollbar) | drop | nothing — native OS scrollbars in v1. Not a component: effect-only (`return null`), fails the §6.1 atom test, app-shell infrastructure — never Storybook material. If brand color is ever wanted later: CSS `scrollbar-color` tint in `globals.css`, zero JS (board Option B) | canvas chat fb-40, 2026-08-04 (`.claude/plans/scrollbar-verdict.plan.md`) |
| `lucide-react` dependency + footer's hand-inlined Instagram/TikTok SVGs + per-call-site glyph pasting | rewrite | `ui/Icon` — `<Icon name>` over a name-keyed registry in `src/assets/glyphs/` (one TS file per glyph: phone/instagram/tiktok v1, source+license comment each; stroke/fill mode absorbed internally), bare `<svg>` viewBox-24 root, `currentColor`-only, `aria-hidden` default (non-empty `aria-label` ⇒ `role="img"`), rem presets sm/md/lg; no icon package (§3) — paths vectorized. Deferred glyphs (menu, x, chevrons, map-pin, clock, whatsapp, star) arrive with their consumer sections | plan canvas fb-80, 2026-08-05; glyphs → assets folder fb-84; public/-vs-inline consultation fb-77/83 |
| top-bar's inline nav anchors (`top-bar.tsx:119-137`; absorbs the dropped `ui/link`'s `nav` look) | rewrite (pattern, not a 1:1 component) | `ui/TextButton` — the general quiet action: slot children, `active` → `aria-current="page"` + static 2px underline + green label, `asChild` via shared `ui/slot.ts`; **ONE green** for hover label + active label + underline = `cta-hover` #006b42 (`cta` #008854 = 4.29:1 on `--page`, fails SC 1.4.3 as a rest state — the old top bar's single-`accent` pattern preserved on the AA-passing shade); D5 exception clocks 200ms color + 300ms sweep ease-out (deliberately NOT the system 400ms `--fade`; tests pin the exception in both directions), both motion-reduce-disabled; min-h-11, no widths, no margins; old click-hijack (`preventDefault` + JS router) and hardcoded `target/rel` rewritten away — the atom never owns navigation | dossier fb-125 + ledger Approve fb-130, 2026-08-05; canvas loop 2026-08-06: one-green active amendment (owner feedback) + pack approved; sealed on owner "create pr" 2026-08-06 |
| `composite/wordmark` (LogoMark + brand text row) | merge | Header's brand slot — one home link, interim `clinic.name` in `--font-display`; the vectorized Publio SVG swaps in when supplied (§15.6, declared diff). ⚠ 2nd old consumer `composite/footer`: the Footer run re-answers its brand corner (the §4 promotion moment if it wants the same link) | Header board D3 fb-90/94/107 + breakdown ledger Approve fb-130, 2026-08-05 (`.claude/section-runs/2026-08-05_22-04_top-bar/`) |
| `ui/logo-mark` (generic tooth SVG) | drop | nothing — it is not the brand; the Publio logo replaces the whole corner (§15.6). Smells recorded: px `size` prop emitting width/height (§7); the tooth's "hole" is `fill-bg-elevated`, hardcoding ground color into the glyph (the `currentColor`-only rule in `ui/Icon` exists because of this bug class) | Header board D3 fb-107 + ledger Approve fb-130, 2026-08-05 |
| `ui/link` (external-link atom; default/muted/nav variants) | drop | nothing as a component — **0 app consumers** (rule 1; grep 2026-08-05). The `nav` look continues as `ui/TextButton` (general quiet action, fb-125). Smells: hardcoded `target="_blank" rel` on every link; free-string variant map | breakdown ledger Approve fb-130, 2026-08-05 |
| top-bar's lucide `Menu`/`X` usage | drop (dependency usage, not a component) | burger = one bespoke `aria-expanded`-driven three-line morph SVG child inside GlyphButton (D12); the `x` registry glyph arrives with ContactModal's close button | Header board D12 fb-104/120 + ledger Approve fb-130, 2026-08-05 |
| `composite/floating-book-cta`'s **placement** duty (the look already moved into `ui/GlyphButton` at fb-46/47) | rewrite | `sections/FloatingActions` — the fixed corner pair: language pill bottom-left, `tel:` call CTA bottom-right (`GlyphButton asChild solid/round/lg` + `Icon phone`), both `fixed z-40`, plus the clearance spacer that keeps the last content line off them at 320px (§9). **First `sections/` component in the repo**, so it sets three precedents: `useTranslations`/`useLocale` in a Server Component with no `'use client'` (isomorphic — the shell's async `getTranslations` would be un-storyable; proven zero-JS by a real `next build` chunk scan), a Fragment root rather than a z-40 wrapper (no stacking context, no pointer-event swallowing), and the mount contract — it must render **after** `{children}`/footer or the spacer clears nothing. v1's language pill is **inert by construction** (plain `<p>`, `aria-hidden`, no role/handler): it IS the language button, only the swapping behaviour is deferred to the Phase 3 LanguageSwitcher. No new message keys — reuses `common.actions.call` ×5 | dossier `.claude/section-runs/2026-08-05_22-04_top-bar/sections/FloatingActions.md` (D13, fb-106/110/129); N2 contract board fb-131→134, 2026-08-12; a11y F1 risk re-confirmed by owner fb-136; sealed on owner "create pr" 2026-08-12 |
| `ui/text` (`variant: body\|lead\|small\|muted`, `as: p\|span\|div` — `as` never used by any consumer; `lead` carries a §6.5 `sm:` self-scaling smell) | rewrite (v1 deliberately narrowed: no `small`/`lead` — both had real old consumers (blog/pricing), but §15.1's 1.125rem body base makes 1rem the "smaller" step already; `div` dropped as dead non-semantic API; `dt`/`dd` added — new coverage the old union couldn't reach) | `ui/Text` — `tone default\|muted\|strong` (fb-182; `strong` speculative, no consumer yet), **first tag-generic atom**: `as p\|span\|dt\|dd` via `createElement` + explicit internal annotation (JSX-spread provably uncompilable on ref variance; zero casts), `text-base` only with leading inherited from the utility (D2 — pinned by fb-186's zero-visual-diff Footer-swap criterion; future lever is `--text-base--line-height`, a declared-global change), always-explicit ink — no `inherit` tone (D4; rewiring passes computed tone per element), no `aria-label` prop by design (D6 — non-interactive, text children); **weight axis** `bold?: boolean` → `font-bold` (700 — true bold, variable font registered 200–900) — additive on the untouched fb-182 tone triple, orthogonal to tone/`as`, CSS-only (no `<strong>`), default `false` | contract canvas v2 fb-189, 2026-08-19 (`text-atom-contract-v2.plan.md`, supersedes the aborted 2026-08-18 draft — salvage report in its §6); G2 react+ts reviewers both Approve (probe-verified, 0 blocking); V manifest-exact (8 darwin baselines, 65/0); pack approved in chat ("code looks very good"), sealed on owner "create pr" 2026-08-19; **weight addendum v3** approved in chat 2026-08-19 ("agree") — `text-weight-addendum.plan.md`, label board Q6; G2 react Approve + ts Clean (0 blocking, 2 NITs advisory); V manifest-exact 67/0 (+2 Bold baselines, six frozen stories byte-identical); pack approved + sealed on owner "add commit to branch and pr" 2026-08-19, second commit on the open lane (PR #37) |
| `ui/heading` (`level` + `visualLevel` as independent axes — right instinct, wrong lock: `Tag = h${level}` hardcoded, so a `<p>` title / `<a>` brand host is impossible by construction; `sm:`/`lg:` per-viewport self-scaling smell §6.5) | rewrite | `ui/Heading` — the display-type step with the ELEMENT left entirely to the consumer: `size` role-named union, v1 ONE step `'title'` = byte-exact `font-display text-xl text-ink-strong` (zero-diff-rewire invariant — the Footer ×3 + Header brand rewires, their own lanes, are accepted on ZERO visual diff; no `leading-*`/`tracking-*`/margins ever); bigger steps (`section`, `page`) join additively only with measured consumers, default pinned `'title'` forever (§6.6 silent-break guard); default host `<p>` (the dominant real consumer — Footer titles open no outline slot on purpose), real h1–h6 / the brand `<a>` become the heading via `asChild` over shared `ui/slot.ts` (no disallow set — even `id` merges down, for a future section's `aria-labelledby`); ink fixed `ink-strong` (all four call sites; the inverse-ink hero arrives as Heading's own additive prop with that consumer, never a caller override §6.8); zero-JS server-safe (no `'use client'`, render-time slotClone) | contract board v2 approved (owner, terminal) 2026-08-19 (`heading-atom-contract-v2.plan.md`); G2 react+ts reviewers Approve; V manifest-exact — 7 darwin baselines (5×1280 + 2×320 stress), full net 64/0; pack `heading-pack.html` + seal on owner "create pr" 2026-08-19 |
| `composite/footer` (brand block + link columns + ANPC row + copyright; sm:/lg: viewport self-scaling, `opacity-60` dimming, glued copyright fragments, two ANPC badges) | rewrite | `sections/Footer` — the shell's closing band as a ZERO-ISLAND Server Component (no `'use client'`, no `<button>` anywhere — every control is a link; the FloatingActions isomorphic-t() precedent at 4× the size). Brand row: plain `<p>` (Header owns the home link; a repeated band claims no outline slot), interim `clinic.name` in display type. ONE `@container` gutter box (the Header pill's exact clamp, second consumer by copy — rule-of-two noted in-file) with named steps only: info grid 1 → `@3xl` 2 → `@5xl` `[1fr_0.6fr_1fr_1fr]`, German-calibrated. Columns: contact (muted tagline/label, `TextButton asChild` tel: link — E.164 href, human text, `-ml-2` optical alignment — crawlable NAP §10.5, county dropped when = city); site-map `<nav>` named by its visible title via `aria-labelledby` (routes from NEW `lib/routes.ts` — the ONE list `useNavItems` now also consumes, fb-181; blog filtered per-locale by the module); ANPC/SAL badge (row above); per-day hours `<dl>` from NEW `lib/hours.ts` (Intl weekday names against a fixed reference week, one row per day owner 2026-08-18, closed rows dimmed by TONE token never opacity). Legal strip: ICU copyright (year crosses as string — no digit grouping), `#top` back-to-top riding the new motion-gated `scroll-behavior: smooth` (+ `data-scroll-behavior` on `<html>`, the Next 16 route-transition guard), socials via `GlyphButton asChild` from `clinic.social` — `sameAs` now DERIVED from it (§10.1 one level deeper). Typography rides `ui/Text` (tone axis; per-element `as="dt"/"dd"`) + `ui/Heading` (title step) — the fb-186 rewire landed IN-lane on zero visual diff (8 pre-rewire darwin baselines pass unchanged). Support: `clinic.ts` gains `SchemaDay` closed union + optional-by-design `social`; +9 `footer.*` keys ×5 locales | N2 composition contract fb-179 approved 2026-08-17 + owner amendments 2026-08-18 (text-xl titles, per-day rows, contact gap-3, socials-in-strip); G2 react+ts review rounds in-lane (data-scroll-behavior HIGH fixed, SchemaDay MEDIUM typed); fb-186 atoms rewire post-merge, accepted on zero diff; gates: vitest 408/408, full visual net 82/0 darwin, a11y per-story clean; aspect + code approved in chat, sealed on owner "create pr" 2026-08-20 |
| — no old-repo counterpart (`ui/Image` is the §11 single wrapper, born as a Phase 0 pass-through; the old repo used raw `<img>`s — its hero/doctor-card/wordmark read as REQUIREMENTS only) | rework (new-repo atom) | `ui/Image` v2 — ONE new axis `variant plain\|framed\|artwork` (D1: situations, not CSS knobs; split rejected — presentation stays a lookup-table row, behavior would leave the atom): `plain` = byte-exact pass-through, default pinned forever (D5, the deferred gray-out overlay joins additively); `framed` = `h-full w-full rounded-xl object-cover` — 12px radius INTERNAL, not a prop (D2/D3, fb-193/195/196: `variant="framed"` IS the direct input); `artwork` = `h-auto max-w-full object-contain` + `placeholder="empty"` default with explicit-prop-wins (D4 — no blur ghost under transparency); no `ratio`/`radius` props (D6); `alt` required by types, decorative `alt=""` storied (D7); models Next 16 `preload` (`priority` remains a deprecated pass-through alias); **D8**: `build-storybook` runs the optimizer first — until now the visual net photographed ExportedImage's 404 error-fallback, never the real pipeline; first tests (24, each story additionally running as a per-story axe test) + 6 stories incl. 2× stress-320; demo fixtures `public/images/demo/` committed, `placeholder-hero.png` deleted (Q4) | contract board v2 `image-variants-contract-v2.plan.md` approved on canvas fb-190…fb-199, 2026-08-20 (supersedes v1 `image-rework.plan.md`, approved 08-18, parked on builder 529s); branch renamed `rework/image` (Q5 delegated, fb-199) |
| — no old-repo counterpart (ancestor `composite/wordmark` merged into the Header brand slot — row above anticipated this "§4 promotion moment"; resolved SHARED SECTION per the SectionHeading-paradox rubric) | new (section) | `sections/Wordmark` — the brand corner as ONE component consumed by Header AND Footer (first cross-section import): placeholder `<a>` with NO href (D9, fb-200 — clickable-shaped, navigating nowhere; no aria-label, prohibited-ARIA avoided; wiring = declared 2-line diff: i18n Link + `brand.ariaLabel`, key reserved ×5, currently uncalled); artwork is a PROP `WordmarkArtwork {src,width,height}` (readonly, D11 "src image is just parameter") defaulting to the committed 256×171 ~9.9 KB WEBP derivative (F12 — serves in all four paths: dev/vitest/storybook/build; the 585 KB PNG stays as the ui/Image demo fixture); NO middle bar (D12 "remove vertical line", owner live in Storybook — supersedes the v2 bar; F11's shrink-0/silent-shock-absorber lesson recorded on the board); zero islands, zero t(), zero new message keys; ONE `@max-xs` container tighten step (gap-3→2, artwork 90%→40%) resolving against the CONSUMERS' containers (documented precondition) — 203px cell / 17.94px slack one-line @390 (fb-207), two-line wrap @320 (147px cell, 17.08px min-content slack), SC 1.4.12 probed PASS; in-lane swaps: Header brand Link → Wordmark (interim home-click REMOVED per D9, deliberate), Footer brand Heading → centred `h-16` box (fb-205; fb-179 moot while inert); truth-fix comments in NavMenu + HeaderNav (focus-return retargets to first nav link, asserted); test infra: `src/types/raw-import.d.ts` narrow `*?raw` shim (F7 — program-wide vite/client ambient leak reverted), comment-stripped source guards, import-surface allowlist, Footer lockup-chain play-fns both branches | N2 board `brand-lockup-contract.plan.md` v2 fb-200…fb-208 + owner terminal rounds D11/D12; models per owner D8 override: builder + G2 reviewers Opus 5, Fable main-loop verification (standing Fable-reviews rule untouched elsewhere); G2 two rounds: 2 HIGH (program-wide vite types · sub-pixel-crushed bar) + 6 MEDIUM found → all fixed/ruled, round-2 approve; gates vitest 457/457 · visual 93/0 darwin (21 declared brand-corner baselines) · axe clean · 1.4.12 probe; sealed on owner "create pr the code is fine" 2026-08-21 |
| — no old-repo counterpart (the brief’s §9 ContactModal spec is the requirement; the old repo’s only `role="dialog"` was its mobile menu, `top-bar.tsx:180`, already superseded by NavMenu’s disclosure) | new (atom) | `ui/Modal` — native `<dialog>` + `showModal()` (top layer, inert-outside, Esc and focus return from the platform — no z-index, no focus trap), controlled `open`/`onClose` (exactly once per close path), free `header` slot + always-on ✕ (`GlyphButton` + `Close` glyph; `closeLabel` REQUIRED, no default — nothing Romanian in the atom), width `sm\|md\|lg\|xl\|full` / height `auto\|sm\|md\|lg\|full` steps with container-query padding, `dimBackdrop` = the locked `--scrim` on `::backdrop` (+ one token-layer line so pre-2024 engines keep the dim), `closeOnBackdropClick` (rectangle check + drag guard; Esc always closes), `initialFocusRef`; `lib/scroll-lock.ts` born here, NavMenu adopts it in a later lane; `sections/ContactModal` (next run) composes it. **Amended 2026-08-28 (the `scrollable` rework, ContactModal lane, owner-approved fb-298):** new prop `scrollable?: boolean` — default `true` keeps the original behaviour (the box capped at the viewport minus 2rem, its body scrolling and becoming a `role="region"` tab stop when it overflows); `false` means the box is NEVER capped and its body never scrolls — a box taller than the viewport makes the dialog's own full-viewport LAYER scroll instead, so the box moves whole under the scrim with the scrollbar at the viewport edge, and the height steps become MINIMUMS in that mode (a fixed step would clip with nothing left to scroll — G2 HIGH). Structure amended (D2·A′ → D17): the `<dialog>` IS a transparent full-viewport layer (`fixed inset-0 p-4 flex flex-col overflow-y-auto overscroll-contain`) and the white box its only child (`@container m-auto shrink-0 …`); `className` targets the box, `ref`/`aria-*`/`rest` the dialog; backdrop press = a press starting AND ending on the layer itself, excluding the layer's own scrollbar band (D19 — the D11 rectangle check retired); the page scroll lock is unchanged in both modes (D20); focus on open lands on the layer, so arrow keys scroll it when the box is taller than the viewport (D21). New story `NonScrollable` (stress-320, baselines at 1280 + 320); all 17 prior baselines pixel-identical — the layer's `p-4` is exactly the margin the old `min(100% - 2rem, step)` width formula carried. Board `.claude/plans/modal-scrollable-rework.plan.md` (D16–D24) | canvas fb-261, 2026-08-26 — five annotation rounds (fb-221–260); D10 `closeLabel` required per fb-259, D15 scoped to the atom per fb-260; **scrollable rework D16–D24 approved fb-298 + "agree with plan", 2026-08-28** |
| — no old-repo counterpart (no language UI ever existed there — the `$lang` route only parsed the prefix; requirements = brief §5, §8.5–8.7, §9, §16.2 + the owner's description of 2026-08-27) | new (atom) | `ui/SpeedDial` — the mercury-thermometer chooser: one filled `<button>` BULB (the current option; state-invariant `aria-label` REQUIRED by the types + `aria-expanded`/`aria-controls`) unfolding an always-mounted `<ul role="list">` STEM of one-step-smaller discs (`inert` + `inert:invisible` belt + `clip-path` stencil while closed — D8 = M; 300ms sweep + 60ms stagger, visibility transitioned asymmetrically so the closing film plays and opening is instant, `motion-reduce` instant — D9) in `direction up\|down\|left\|right` (D6; nearest-the-bulb first = HTML order = Tab order, letters never rotated — D7); each disc is an `<a href lang hreflang>` when its option carries an href and a `<button>` when not (D2 = B′), `onSelect(option, event)` fired from onClick WITHOUT preventDefault (the section writes its cookie there and can skip it on a modified click — the atom stores nothing, §12 test); model C (D1: the bulb keeps `value: NoInfer<V>` — a typo fails to compile —, stem = `options` minus it, no sorting); `size md\|lg` is the FALLBACK of the public `--disc-size` variable a host sets per screen type (D16 · F2), stem discs 32/44 · 44/56, tube = bulb − 2px (D12, ring-fit); a second public variable `--stem-inset` feeds the extreme-zoom cap (measured from the bulb's centre); `tone ink\|cta` named bundles, default `ink`, colouring the BULB only — the stem discs wear GlyphButton's ghost manner plus the resting `--line` ring (D5's inset-shadow creep REVERSED by the owner in the live Storybook review, 2026-08-27: "drop it and use just old on hover" → "ghost glyph but with a border"); closes on bulb tap · Esc (focus back after the commit) · outside pointerdown · focus-out · pagehide/pageshow-persisted (D11); uncontrolled `open` + `onOpenChange` (D13); dev-only tripwires for Label-in-Name (option label ∌ code, bulb name ∌ code). Born with it: `ui/disc.ts` (D17 Road 3 — `discBase` + `discBox` shared with GlyphButton, which is rewired on ZERO pixel change: every `ui-glyph-button--*` darwin baseline byte-identical; its glyph follows the same variable at the exact fractions 5/11 and 1/2), the visual runner's `pin-open-hover` tag (shared `openDisclosure()`), `tests/unit/locales.test.ts` (every endonym starts with its code — SC 2.5.3 holds by data), one jsx-a11y `no-redundant-roles` exception (`ul/ol: ['list']`, WebKit); 15 stories → 16 darwin baselines under `ui/speeddial/`; first consumer `sections/LanguageSwitcher` (Lane B: passes `--disc-size` steps + `--stem-inset` on the corner, wraps in `<nav aria-label>`) | canvas `.claude/plans/language-dial.plan.md`, 2026-08-27 — five annotation rounds (fb-262–297), D1–D17 all decided; G2 (react · typescript · a11y, Fable) one HIGH (`NoInfer`) + six MEDIUM fixed in-lane and re-verified; builder details recorded in the S7 pack (letters = bulb × 2/7 with a 0.875rem floor; exact one-step fractions instead of × 0.786; href-less pick closes + returns focus; position-free root so a host's `fixed` wins the cascade; `overscroll-contain` dropped; 2.5.2 pointerdown dismissal = D11 accepted deviation) |
| — no old-repo counterpart (no language UI ever existed there — the `$lang` route only parsed the prefix; requirements = brief §5, §8.5–8.7, §9, §14, §16.2 + the language-dial board) | new (section) | `sections/LanguageSwitcher` — the floating language dial, a `'use client'` island of exactly ONE element: `<SpeedDial size="lg">` (tone default `ink`, D4) fed by `useLanguageOptions()` — the five locales in manifest order, current one included (model C, D1), each `{ value, code: toUpperCase() in JS (fb-133), label: endonym (§8.5, static data), lang, href: localeHref(target, equivalentPath(pathname, target)) }`; the bulb's spoken name = the section's ONE key `common.language.switch` (`{name} · schimbă limba` ×5, endonym first so the visible code is contained in it — SC 2.5.3, D10/D15 fb-281); `onSelect` writes the site's ONE cookie `NEXT_LOCALE=<locale>; path=/; max-age=31536000; SameSite=Lax` on the explicit unmodified click only (meta/ctrl/shift/alt-clicks write nothing — the atom hands the event over for exactly that, D2), never on mount (§8.7 regression test), no `preventDefault` — the browser performs the full document load (§15.13); no `<nav>` landmark (parked owner question — it would need a second owner-authored key). Lifted into `lib/routes.ts` on the rule of two: `matchesRoute` (the Header's former private `isActive`, zero pixel change — every `sections-header--*` baseline byte-identical) + `equivalentPath` (same path over there, else `/`; NOT a whitelist — only a route scoped to another locale is missing, today `/blog` off `ro`, §5; the row shape "no trailing slash" pinned by test). **FloatingActions swap** (the fb-129/136 inert-pill risk closes): the `<p aria-hidden>` placeholder → `<LanguageSwitcher direction="up">`; the D16 · F2 size steps on BOTH corners as one token list `[--disc-size:3.5rem] xl:[--disc-size:4rem] 2xl:[--disc-size:4.5rem]` (56 · 64 · 72px), `--stem-inset: calc(6rem + env(safe-area-inset-bottom))` on the dial (1rem offset + the Header pill's 5rem reach), the spacer stepping 5.5 → 6 → 6.5rem (its `scroll-padding-bottom` shell debt now steps too); FloatingActions stays a Server Component that mounts the island; its stories pin reader + route together (a toolbar-only DE flip is a state production cannot reach) and gain `GermanOpen` — the only baselines of the open dial IN the corner. Stories `Sections/LanguageSwitcher` Default + Open (`pin-open`) + Picked through a demo host that cancels a pick's navigation in the bubble phase (after the cookie write) and re-renders the dial as the next page would (owner's Lane A ask — no dead-URL error page in Storybook); 9 new darwin baselines (`sections/languageswitcher/{default,open,picked}` at 390 + 1536, `sections/floatingactions/german-open` at 320/390/1536) + 5 re-recorded FloatingActions cells = the manifest as amended at G2, everything else zero diff | dossier `.claude/section-runs/2026-08-05_22-04_top-bar/sections/LanguageSwitcher.md` (approved, rewritten 2026-08-27 from the board `.claude/plans/language-dial.plan.md` fb-262–297); D15 values owner-approved fb-281; G2 (react · typescript · a11y, Fable) 0 CRITICAL / 0 HIGH — 1+0+2 MEDIUM and 5+5+4 LOW verified, 11 applied in one builder round (stories pin reader+route, `GermanOpen` + `Picked` stories, demo strips the base path, static-markup crawler probe, routes row-shape guard, comments), landmark + comma parked as owner questions, atom zoom-cap squeeze + stale 3-modifier doc + root-redirect `lang` + 404 hydration recorded as follow-ups; gates at READY: tsc/lint/prettier clean, vitest 738/738 (36 files), V 135/135; pack `.claude/plans/language-switcher-pack.html` on the canvas 2026-08-28, approved in chat ("code looks good, create pr"); owner questions left as shipped: `<nav>` landmark, comma vs "·", CLAUDE.md §15 record line, cookie `Secure`; sealed on owner "create pr" 2026-08-28 |
| — no old-repo counterpart (the brief's §14 "Contact (modal)" + the §9 ContactModal spec are the requirement; the old repo never had a contact dialog — its only `role="dialog"` was the mobile menu, superseded by NavMenu's disclosure) | new (section) | `sections/ContactModal` — the call-the-clinic dialog as ONE client island in three exports: **`ContactModalProvider`** (the page's single `open` boolean; renders `{children}` untouched, then the dialog ONCE — one `<dialog>` per document however many openers; `defaultOpen` for stories/tests), **`useContactModal()`** (context hook, throws outside a provider; the context lives in the hook module to break the provider↔dialog import ring) and **`ContactModalTrigger`** (a `ui/Button` that opens on press, `aria-haspopup="dialog"`, `asChild`/`aria-haspopup` removed from its props BY TYPE and owned after the spread; the label is the consumer's — the Header will pass `common.actions.contact`). The dialog composes `ui/Modal` (`width="md"`, `scrollable={false}`, no `height`) with the `<h2>` `contact.heading` CENTRED IN THE TOP BAR beside the ✕ (a `ps-14` wrapper cancels the 44px ✕ + 12px gap; it names the dialog via `aria-labelledby` + `useId`) over a centred column: `contact.lead` and ONE `<a href="tel:">` in `ui/Button` solid-lg clothes (Phone glyph decorative → the link's accessible name IS the visible number, SC 2.5.3; E.164 href / display text from `lib/clinic.ts`, §10.1) — nothing under the control. Owner rule: the dialog NEVER scrolls — `scrollable={false}` leaves the box uncapped and the ~200px card is shorter than every §7 viewport AND a phone held sideways, so nothing scrolls anywhere (tests load Source Serif 4 and assert the atom's overflow markers are absent at RO 320×568 / RO 390×844 / DE 320×568 / 844×390). `text-center` sits on each paragraph (the base layer's `p { text-align: start }` beats inherited centring). Keys: +2 `contact.*` ×5 locales (`heading`, `lead`; `close` reused), `contact.title` retired ×5. Tooling in the same lane: `.storybook/manager.ts` (addons panel docked right) + `.storybook/manager-head.html` (the viewport tool's phone frame keeps its declared height instead of being squashed to the canvas, plus a 40rem canvas floor) — Storybook's own geometry was capping this top-layer dialog and making it look scroll-locked. NOT wired: Phase 4 mounts the provider in `layout.tsx`; a rework lane swaps the Header bar CTA + NavMenu panel CTA to `ContactModalTrigger` and must close the menu and open the dialog in ONE commit (the menu's `inert` freeze would otherwise freeze the dialog); the corner disc stays a direct `tel:` | N2 ran as an EXPLORATION (12 atom-only candidates + canvas pack) → owner picked #9 + a schedule → composition board `.claude/plans/contact-modal-n2-contract.plan.md` approved "agree" 2026-08-27 (D1–D15, EN/DE/FR/IT drafts adopted) → owner amendments 2026-08-27/28: title into the bar, viewport pins dropped, `ui/Modal` `scrollable` rework (fb-298), and finally the second label + the whole schedule DROPPED (keys `note`/`hoursTitle`/`closed` retired, `lib/hours` restored to develop — no speculative helper ships). G2: react + typescript + a11y (Fable) 0 CRITICAL/HIGH on the section, 1 HIGH + 3 LOW on the atom rework, all applied and re-verified. Gates: vitest 613/613, visual net 119/119 (7 section baselines + 2 `ui/modal--non-scrollable`), axe 0, parity green |
| `ui/eyebrow` (clean leaf, but: arbitrary `tracking-[0.18em]`; stale `text-fg-muted` token; `as` offered p\|span\|div while typing props `ComponentPropsWithoutRef<"p">` — the variance hole `ui/Text` D5 later solved; two hand-rolled copies bypassing it — `reviews-carousel.tsx:273` verbatim on an `aria-live` region, `review-card.tsx:88` a DIVERGENT 0.6875rem/0.12em credential) | rewrite | `ui/Eyebrow` — the mono micro-label above section titles, sole consumer of `--font-mono` (§3 "eyebrows/micro-labels"): ONE step, ONE prop (`children`, required finished text §8.1), host a hardcoded `<p>` — **no `as` axis** (owner fb-300 senior call: every real site is a `<p>`; joins additively, default pinned, if an inline consumer ever lands); recipe `font-mono text-sm font-medium tracking-widest text-ink-muted uppercase` — `text-sm` (owner 2026-09-01: 14px against the §15.1 18px body restores the old 0.75 eyebrow-to-body ratio; `text-xs` would shrink it, §1 older audience) + `tracking-widest` (owner 2026-09-01: stays on §3's untouched default scale — repo has zero arbitrary tracking values; deliberately tighter than the old 0.18em); uppercase is CSS never the string (messages stay sentence-case, the browser owns Ș/Ț case mapping — test-pinned); ink fixed `ink-muted` (7.35:1 on white); zero-JS server-safe; double type-pin against `as` returning (`@ts-expect-error` + `expectTypeOf`, negative-probe verified). Parked: review-card's second recipe = an additive `size` step only when the ReviewCard lane measures it; the carousel's copy-paste rewires to this atom in its own lane | classified 2026-09-01 (/classify-component rule 2; sibling verdict: `composite/section-heading` → section, early wave, blocked on this merge); tracking+size terminal Q&A 2026-09-01; pack canvas rounds fb-299…fb-304 (fb-300 removed the `as` axis — 7 surviving baselines byte-unchanged); G2 react+ts reviewers (Fable/max) **Approve ×2, nothing above LOW**, 3 LOWs applied (truthful merge-order comment · on-white contrast wording · surgical `expectTypeOf` pin); gates 802/802 · V full net 151/0 · 7 darwin baselines; sealed on owner "create pr then, looks good" 2026-09-01 |
